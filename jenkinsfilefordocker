node
{
stage('scm checkout'){
git 'https://github.com/devinameena/maven-project'
}
stage('package stage'){
sh lable: ",script: 'mvn clean package'
}
stage ('Checkout to different branch') {
sh "git branch -r"
sh "git checkout master"
}
}
